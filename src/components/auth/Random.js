import styled from 'styled-components';
import AuthTemplate from './AuthTemplate';

/**
 * 필요시 아무거나 작성할 수 있는 페이지
 */

const RandomBlock = styled.div``;

const Random = () => {
    return <AuthTemplate></AuthTemplate>;
};

export default RandomBlock;
